name: smartmart_dbt_project
version: '1.0'
config-version: 2
 
profile: smartmart_profile   # ensure a matching profile exists in your profiles.yml
 
model-paths: ["models"]
seed-paths: ["seeds"]
snapshot-paths: ["snapshots"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
 
source-paths: ["models"]    # sources.yml lives under models/ per above
macro-paths: ["macros"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_modules"
 
models:
  smartmart_dbt_project:
    staging:
      +materialized: view
    intermediate:
      +materialized: table
    marts:
      +materialized: incremental
    dims:
      +materialized: table
